# GraphQL API endpoint
NEXT_PUBLIC_GRAPHQL_API_URL=

# Public site URL (used as origin fallback in the bookings route)
NEXT_PUBLIC_SITE_URL=

# GraphLand payment gateway credentials (server-only, used in src/lib/pgw.ts)
GRAPHLAND_PGW_API_KEY=
GRAPHLAND_PGW_CLIENT_ID=
